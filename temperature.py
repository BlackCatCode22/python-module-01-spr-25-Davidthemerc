# Accept celsius temperature as input
temperature = float(input("Enter temperature in Celsius: "))

# Convert celsius to fahrenheit
temperature = temperature * 9 / 5 + 32

# Print fahrenheit temperature
print(temperature)